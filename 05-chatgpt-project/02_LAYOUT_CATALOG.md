---
title: "Layout and Infographic Catalog"
description: "Selection index and full specifications for layouts L01-L48 and infographics I01-I12."
document_type: project-knowledge
status: active
---

# Layout and Infographic Catalog

Dùng file này sau khi đã hiểu nội dung và topology của slide. Đọc chỉ mục trước, sau đó chỉ đọc sâu phần L/I được chọn. Các tên ảnh trong chỉ mục tương ứng với PNG trong thư mục images của gói upload.

---

## Chỉ mục lựa chọn L01–L48 và I01–I12

### Chỉ mục bố cục và ảnh tham chiếu

Đây là chỉ mục lựa chọn duy nhất. Chọn intent và topology trước, sau đó mở **đặc tả**, **preview riêng** và **ảnh mẫu thật**. Không đánh giá từ contact sheet hoặc tên file. Với ảnh mẫu, ghi rõ học điều gì và không học điều gì trong deck plan.

#### Cách chọn

1. Nhóm ngang hàng → grid/card; trình tự → process/timeline; vòng lặp → cycle; cấu thành → anatomy/layers/tree; đối chiếu → comparison/matrix; số liệu → chart/dashboard; thực hành → task/scorecard.
2. Chọn `L` khi cần cấu trúc quan hệ; chọn `I` khi cần cấu trúc kèm hình neo, icon và ví dụ thị giác.
3. Mở đặc tả của mã và PNG/SVG tương ứng. Ảnh thực chỉ dạy geometry, spacing, palette, icon, material và hierarchy; không cung cấp facts, text, logo hoặc claim.
4. Chốt density và style sau topology. Không tạo mã mới chỉ vì đổi màu.

#### L01–L48 — bố cục cấu trúc

Mỗi liên kết mã mở đặc tả; `PNG` là preview riêng ở kích thước đầy đủ. Cột ảnh thật là ví dụ gần nhất để quan sát, không phải nguồn nội dung.

| Nhóm | Mã, đặc tả và preview | Ảnh thật nên mở | Học / không học |
|---|---|---|---|
| G01 Mở đầu & định hướng | L01 PNG · L02 PNG · L20 PNG · L30 PNG · L33 PNG · L37 PNG | Nền navy, R07 | Học vùng trống/nhịp; không lấy chữ hay claim. |
| G02 Khái niệm & khung tư duy | L03 PNG · L10 PNG · L24 PNG · L25 PNG · L26 PNG · L38 PNG | R04, R03 | Học phân tầng/cụm; không coi mô hình trong ảnh là tài liệu chức năng. |
| G03 Quy trình & thời gian | L05 PNG · L12 PNG · L17 PNG · L21 PNG · L22 PNG · L34 PNG | Quy trình, R05, R06 | Học nhịp thẻ/connector; không lấy lời hứa thực thi hay tự sửa luật. |
| G04 So sánh & quyết định | L04 PNG · L06 PNG · L16 PNG · L19 PNG · L31 PNG · L40 PNG | So sánh, R02 | Học baseline/cột/bậc; không dùng tỷ lệ 10/40/75/95% như dữ kiện. |
| G05 Kiến trúc & điều hành | L07 PNG · L11 PNG · L15 PNG · L27 PNG · L42 PNG · L43 PNG | R03, R04 | Học tầng/trụ/điểm kiểm; không sao chép tên hệ thống, logo, facts. |
| G06 Dữ liệu & bằng chứng | L23 PNG · L28 PNG · L29 PNG · L35 PNG · L44 PNG · L45 PNG | Giao diện | Học vùng/callout; không coi UI mô phỏng là screenshot thật hay dùng số mẫu. |
| G07 Giải thích & khám phá | L08 PNG · L13 PNG · L14 PNG · L32 PNG · L36 PNG · L39 PNG | Tài liệu, Giao diện | Học chú giải/scene; không bịa schema, persona hay tính năng. |
| G08 Thực hành & ứng dụng | L09 PNG · L18 PNG · L41 PNG · L46 PNG · L47 PNG · L48 PNG | R05, R07 | Học nhịp hành động; không tự gán người, deadline hoặc hiệu quả. |

#### I01–I12 — infographic minh họa

| ID | Dùng khi / cấu trúc nền | Preview | Ảnh thật phải mở | Học / không học |
|---|---|---|---|---|
| I01 | Vòng lặp có kiểm duyệt; L12/L19 | PNG | R01, R06 | Học vòng, icon, nhấn gate; không lấy chữ/claim miễn nhiễm lỗi. |
| I02 | Bốn bước có hình; L05 | PNG | R05 | Học badge/icon; không lấy body và chân trang lặp. |
| I03 | Ba chặng; L02/L16 | PNG | R07 | Học bệ và mũi tên; không lấy song ngữ/claim hiệu quả. |
| I04 | Năm bước có điểm duyệt; L05/L42 | PNG | R05 | Học nhịp và gate; không tự thêm bước hoặc quyền. |
| I05 | Hồ sơ đến sản phẩm; L17 | PNG | Tài liệu | Học input/output; không bịa schema hoặc automation. |
| I06 | Người–AI handoff; L22 | PNG | R03 | Học phân vai; không coi mô hình là đặc tả sản phẩm. |
| I07 | Năm trụ; L07/L10 | PNG | R04 | Học mái–trụ–nền; không biến trụ thành quy trình. |
| I08 | Ba tầng; L07 | PNG | R03 | Học lớp/icon; không lấy tên tool/facts. |
| I09 | Bốn mức trưởng thành; L16 | PNG | R02 | Học bậc và governance bar; không lấy phần trăm. |
| I10 | Lõi + ba hệ hỗ trợ; L11 | PNG | R03 | Học hub/spokes; không sao chép hệ thống riêng. |
| I11 | Trước–sau; L31/L06 | PNG | R07 | Học chuyển trạng thái; không tự gắn lợi ích. |
| I12 | Bốn năng lực; L10/L03 | PNG | R04, R05 | Học card/icon; không sao chép nội dung nghiệp vụ. |

#### Registry và gallery

- Máy đọc: registry L, registry I, groups, facets.
- Duyệt toàn thư viện: gallery HTML, group previews.
- Ảnh mẫu khác: reference set và user-infographics registry.
- Nguồn lịch sử nằm trong sources, không thuộc đường đọc bình thường.

---

## L01 — Đặc tả bố cục

### L01 — Bìa hoặc chuyển chương

**Họ:** Định vị. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một thông điệp lớn và một hình neo, chuyển nhịp chương.

**Không nên / điều kiện giới hạn:** Không nhét quy trình hay bảng chi tiết vào trang mở.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ TIÊU ĐỀ 60% ] [ HÌNH NEO 40% ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không nhét quy trình hay bảng chi tiết vào trang mở.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L01 — Bìa hoặc chuyển chương.
Mục tiêu: Một thông điệp lớn và một hình neo, chuyển nhịp chương.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không nhét quy trình hay bảng chi tiết vào trang mở.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không nhét quy trình hay bảng chi tiết vào trang mở.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Mở một chủ đề; tạo điểm dừng và định vị thông điệp.

```text
[TIÊU ĐỀ + DẪN NHẬP] | [MINH HỌA LỚN]
```

**Ràng buộc:** Một ý chính, một hình neo; có thể dùng nhánh nền tối. Không áp dụng mật độ của slide kiến thức.

**Ví dụ:** Từ AI tạo sinh đến tác nhân AI.

**Nguồn bố cục:** Kế thừa Archetype 01, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L02 — Đặc tả bố cục

### L02 — Lộ trình học tập

**Họ:** Định vị. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

3–5 chặng, tô rõ vị trí đang học.

**Không nên / điều kiện giới hạn:** Chỉ một chặng active; không giả thời lượng nếu chưa có ngày.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ A ] — [ ĐANG HỌC B ] — [ C ] — [ D ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Chỉ một chặng active; không giả thời lượng nếu chưa có ngày.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L02 — Lộ trình học tập.
Mục tiêu: 3–5 chặng, tô rõ vị trí đang học.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Chỉ một chặng active; không giả thời lượng nếu chưa có ngày.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Chỉ một chặng active; không giả thời lượng nếu chưa có ngày.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Chỉ ra các chặng và vị trí hiện tại.

```text
[01] — [02 ĐANG HỌC] — [03] — [04]
```

**Ràng buộc:** Các mốc tương đương; chỉ tô nổi chặng hiện tại. Đây là bản đồ, không nhất thiết là quy trình thao tác.

**Ví dụ:** Bốn chặng xây dựng môi trường làm việc với AI.

**Nguồn bố cục:** Kế thừa Archetype 02, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L03 — Đặc tả bố cục

### L03 — Giải phẫu khái niệm

**Họ:** Kiến thức. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một định nghĩa nối đến 3–4 thuộc tính.

**Không nên / điều kiện giới hạn:** Đường nhánh là cấu thành, không thứ tự hoặc nhân quả.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ KHÁI NIỆM ] —┬— [ A ]
              ├— [ B ]
              └— [ C ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Đường nhánh là cấu thành, không thứ tự hoặc nhân quả.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L03 — Giải phẫu khái niệm.
Mục tiêu: Một định nghĩa nối đến 3–4 thuộc tính.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Đường nhánh là cấu thành, không thứ tự hoặc nhân quả.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Đường nhánh là cấu thành, không thứ tự hoặc nhân quả.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Giải thích một định nghĩa qua các thành phần hoặc thuộc tính.

```text
[KHÁI NIỆM] → [THUỘC TÍNH 1/2/3/4]
```

**Ràng buộc:** Khái niệm chiếm khoảng 30%; phần giải thích khoảng 65%; 5% cho khoảng nối. Không nối các thuộc tính thành chuỗi thời gian.

**Ví dụ:** Gem gồm vai trò, chỉ dẫn, phong cách và tài liệu.

**Nguồn bố cục:** Kế thừa Archetype 03, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L04 — Đặc tả bố cục

### L04 — Ma trận hai chiều

**Họ:** So sánh. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Phân loại theo hai tiêu chí độc lập và bốn vùng.

**Không nên / điều kiện giới hạn:** Có nhãn cả hai trục; bốn thẻ không có trục không là ma trận.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
      Y ↑
   [ A | B ]
   [ C | D ] → X
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Có nhãn cả hai trục; bốn thẻ không có trục không là ma trận.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L04 — Ma trận hai chiều.
Mục tiêu: Phân loại theo hai tiêu chí độc lập và bốn vùng.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Có nhãn cả hai trục; bốn thẻ không có trục không là ma trận.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Có nhãn cả hai trục; bốn thẻ không có trục không là ma trận.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Định vị nội dung theo hai chiều hoặc một mô hình logic.

```text
[GÓC 1 | GÓC 2]
[GÓC 3 | GÓC 4]
```

**Ràng buộc:** Phải có tên hai trục, ý nghĩa mỗi góc và một kết luận. Bốn thẻ ngang hàng không tự động trở thành ma trận.

**Ví dụ:** Phân loại nhiệm vụ theo mức rủi ro và mức tự chủ.

**Nguồn bố cục:** Kế thừa Archetype 04, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L05 — Đặc tả bố cục

### L05 — Quy trình tuyến tính

**Họ:** Quy trình. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

3–5 bước theo thứ tự, mỗi bước có kết quả.

**Không nên / điều kiện giới hạn:** Đủ bước thật; đầu ra bước trước khớp đầu vào bước sau.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ 1 ] → [ 2 ] → [ 3 ] → [ 4 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Đủ bước thật; đầu ra bước trước khớp đầu vào bước sau.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L05 — Quy trình tuyến tính.
Mục tiêu: 3–5 bước theo thứ tự, mỗi bước có kết quả.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Đủ bước thật; đầu ra bước trước khớp đầu vào bước sau.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Đủ bước thật; đầu ra bước trước khớp đầu vào bước sau.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Hướng dẫn thứ tự thực hiện từ đầu đến cuối.

```text
[01] → [02] → [03] → [04] → [05]
```

**Ràng buộc:** 3–5 bước; mỗi bước có động từ, hình neo, mô tả và đầu ra. Một điểm phê duyệt có thể nhấn cam. Không ép bảy bước thật thành năm bước giả.

**Ví dụ:** Nghiên cứu → Lập kế hoạch → Phê duyệt → Thực hiện → Kiểm tra.

**Nguồn bố cục:** Kế thừa Archetype 05, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L06 — Đặc tả bố cục

### L06 — So sánh và lựa chọn

**Họ:** So sánh. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

2–3 phương án so theo cùng 3–4 tiêu chí.

**Không nên / điều kiện giới hạn:** Không tô một cột là người thắng khi chưa có căn cứ/tiêu chí.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ TIÊU CHÍ ] [ PHƯƠNG ÁN A ] [ PHƯƠNG ÁN B ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không tô một cột là người thắng khi chưa có căn cứ/tiêu chí.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L06 — So sánh và lựa chọn.
Mục tiêu: 2–3 phương án so theo cùng 3–4 tiêu chí.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không tô một cột là người thắng khi chưa có căn cứ/tiêu chí.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không tô một cột là người thắng khi chưa có căn cứ/tiêu chí.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Phân biệt 2–3 giải pháp theo cùng tiêu chí.

```text
[PHƯƠNG ÁN A] | [PHƯƠNG ÁN B] | [PHƯƠNG ÁN C]
```

**Ràng buộc:** Giữ tiêu chí theo hàng: vai trò, đầu vào, cách xử lý, đầu ra, trường hợp sử dụng. Không tô đỏ một lựa chọn chỉ vì nó cũ hơn.

**Ví dụ:** Gemini, Gem và công cụ tra cứu tài liệu.

**Nguồn bố cục:** Kế thừa Archetype 06, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L07 — Đặc tả bố cục

### L07 — Kiến trúc phân tầng

**Họ:** Kiến trúc. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

3–4 tầng trách nhiệm với giao diện trao đổi.

**Không nên / điều kiện giới hạn:** Tầng không có nghĩa tốt/xấu; mũi tên hai chiều cần có phản hồi thật.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ CON NGƯỜI ]
      ↕
[ NỀN TẢNG ]
      ↕
[ CÔNG CỤ / DỮ LIỆU ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Tầng không có nghĩa tốt/xấu; mũi tên hai chiều cần có phản hồi thật.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L07 — Kiến trúc phân tầng.
Mục tiêu: 3–4 tầng trách nhiệm với giao diện trao đổi.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Tầng không có nghĩa tốt/xấu; mũi tên hai chiều cần có phản hồi thật.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Tầng không có nghĩa tốt/xấu; mũi tên hai chiều cần có phản hồi thật.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Mô tả lớp chức năng, luồng thông tin hoặc nền tảng.

```text
[CON NGƯỜI / ĐIỀU PHỐI]
          ↕
[CÔNG CỤ / DỮ LIỆU / MÔI TRƯỜNG]
```

**Ràng buộc:** 3–4 tầng là điểm khởi đầu. Phân biệt đường lệnh và luồng dữ liệu khi cần; vùng bao không được hiểu nhầm là một bước.

**Ví dụ:** Con người → Nền tảng điều phối → Tài nguyên làm việc.

**Nguồn bố cục:** Kế thừa Archetype 07, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L08 — Đặc tả bố cục

### L08 — Tình huống thực tiễn

**Họ:** Ứng dụng. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Bài toán, cách can thiệp và tác động quan sát.

**Không nên / điều kiện giới hạn:** Tách kết quả kỳ vọng với số đã đo; ví dụ phải có nhãn.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ VẤN ĐỀ ] → [ GIẢI PHÁP ] → [ TÁC ĐỘNG ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Tách kết quả kỳ vọng với số đã đo; ví dụ phải có nhãn.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L08 — Tình huống thực tiễn.
Mục tiêu: Bài toán, cách can thiệp và tác động quan sát.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Tách kết quả kỳ vọng với số đã đo; ví dụ phải có nhãn.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Tách kết quả kỳ vọng với số đã đo; ví dụ phải có nhãn.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Gắn một ý tưởng với bài toán, cách làm và tác động.

```text
[VẤN ĐỀ] → [GIẢI PHÁP] → [TÁC ĐỘNG]
```

**Ràng buộc:** Tác động phải có dữ kiện hoặc ghi là kết quả kỳ vọng. Không tự thêm tỷ lệ tiết kiệm hay doanh thu minh họa như số đo thật.

**Ví dụ:** Chuẩn bị tờ trình từ nguồn quy định và biểu mẫu đã duyệt.

**Nguồn bố cục:** Kế thừa Archetype 08, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L09 — Đặc tả bố cục

### L09 — Bài tập thực hành

**Họ:** Giảng dạy. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một nhiệm vụ, dữ liệu, sản phẩm và tiêu chí hoàn thành.

**Không nên / điều kiện giới hạn:** Không đưa toàn bộ lời giải lên trước khi học viên làm.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ ĐỀ BÀI + NGUỒN ] [ SẢN PHẨM ]
[ TIÊU CHÍ HOÀN THÀNH         ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không đưa toàn bộ lời giải lên trước khi học viên làm.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L09 — Bài tập thực hành.
Mục tiêu: Một nhiệm vụ, dữ liệu, sản phẩm và tiêu chí hoàn thành.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không đưa toàn bộ lời giải lên trước khi học viên làm.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không đưa toàn bộ lời giải lên trước khi học viên làm.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Giao nhiệm vụ và tiêu chí hoàn thành.

```text
[ĐỀ BÀI + DỮ LIỆU] | [ĐẦU RA MONG ĐỢI]
[TIÊU CHÍ KIỂM TRA]
```

**Ràng buộc:** Đề bài, đầu vào, việc phải làm và tiêu chí phải tách rõ. Không đưa đáp án vào ảnh nếu mục tiêu là học viên tự làm.

**Ví dụ:** Thiết lập thư mục dự án và soạn bản quy tắc đầu tiên.

**Nguồn bố cục:** Kế thừa Archetype 09, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L10 — Đặc tả bố cục

### L10 — Đúc kết dạng lưới mô-đun

**Họ:** Đúc kết. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một ý chủ đạo, 2–3 ý hỗ trợ và điều kiện quan trọng.

**Không nên / điều kiện giới hạn:** Thẻ lớn là ý quan trọng nhất, không phải thẻ nhiều chữ nhất.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ Ý CHÍNH LỚN ] [ Ý PHỤ A ]
[             ] [ Ý PHỤ B ]
[ ĐIỀU KIỆN / ĐÚC KẾT      ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Thẻ lớn là ý quan trọng nhất, không phải thẻ nhiều chữ nhất.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L10 — Đúc kết dạng lưới mô-đun.
Mục tiêu: Một ý chủ đạo, 2–3 ý hỗ trợ và điều kiện quan trọng.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Thẻ lớn là ý quan trọng nhất, không phải thẻ nhiều chữ nhất.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Thẻ lớn là ý quan trọng nhất, không phải thẻ nhiều chữ nhất.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Tóm tắt các nguyên tắc không cùng mức quan trọng.

```text
[Ý CHÍNH LỚN      | Ý PHỤ 1]
[Ý PHỤ 2 | Ý PHỤ 3 | LƯU Ý]
```

**Ràng buộc:** Một thẻ lớn làm điểm neo; 2–3 thẻ nhỏ. Không chia tất cả thành các ô bằng nhau khi có một nguyên tắc chủ đạo.

**Ví dụ:** Bốn nguyên tắc dùng AI an toàn trong công việc.

**Nguồn bố cục:** Kế thừa Archetype 10, tài liệu 01.

PNG xem trước · SVG · Chỉ mục.

---

## L11 — Đặc tả bố cục

### L11 — Trung tâm và vệ tinh

**Họ:** Kiến trúc. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một lõi phối hợp 3–5 mô-đun.

**Không nên / điều kiện giới hạn:** Không vẽ vệ tinh như thứ tự vòng lặp; tránh đường xuyên chữ.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ A ] ↔ [ LÕI ] ↔ [ B ]
           ↕
         [ C ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không vẽ vệ tinh như thứ tự vòng lặp; tránh đường xuyên chữ.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L11 — Trung tâm và vệ tinh.
Mục tiêu: Một lõi phối hợp 3–5 mô-đun.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không vẽ vệ tinh như thứ tự vòng lặp; tránh đường xuyên chữ.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không vẽ vệ tinh như thứ tự vòng lặp; tránh đường xuyên chữ.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Thể hiện một lõi phối hợp nhiều năng lực.

```text
[LẬP KẾ HOẠCH] ↔ [LÕI AI] ↔ [BỘ NHỚ]
                       ↕
                    [CÔNG CỤ]
```

**Ràng buộc:** 3–4 vệ tinh; chỉ dùng hai chiều khi có trao đổi. Vùng môi trường là đường bao riêng. Nhãn ngắn, ít mô tả ở lõi.

**Ví dụ:** Cấu tạo tác nhân AI: lõi, kế hoạch, bộ nhớ và công cụ.

**Nguồn bố cục:** Mở rộng từ L03/L07 và mẫu trong chuỗi AIA-102.

PNG xem trước · SVG · Chỉ mục.

---

## L12 — Đặc tả bố cục

### L12 — Vòng lặp khép kín

**Họ:** Quy trình. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Quan sát, phân tích, cập nhật có kiểm soát và áp dụng lại.

**Không nên / điều kiện giới hạn:** Phải có đường quay về; sửa luật cần duyệt, không hứa miễn nhiễm lỗi.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ 1 ] → [ 2 ]
  ↑       ↓
[ 4 ] ← [ 3 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Phải có đường quay về; sửa luật cần duyệt, không hứa miễn nhiễm lỗi.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L12 — Vòng lặp khép kín.
Mục tiêu: Quan sát, phân tích, cập nhật có kiểm soát và áp dụng lại.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Phải có đường quay về; sửa luật cần duyệt, không hứa miễn nhiễm lỗi.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Phải có đường quay về; sửa luật cần duyệt, không hứa miễn nhiễm lỗi.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Giải thích phản hồi, kiểm tra, học từ kết quả hoặc tối ưu.

```text
[01] → [02]
 ↑       ↓
[04] ← [03]
```

**Ràng buộc:** 3–5 nút, cùng chiều đọc. Đường trở về phải có ý nghĩa. Chỉ một bước nổi bật; nội dung trung tâm không lặp tiêu đề.

**Ví dụ:** Ghi nhận → Phân tích → Cập nhật → Áp dụng và kiểm tra.

**Nguồn bố cục:** Mở rộng từ L04 và mẫu vòng lặp trong chuỗi trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L13 — Đặc tả bố cục

### L13 — Giải phẫu tài liệu hoặc mã

**Họ:** Giảng dạy. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Tài liệu và 3–4 vùng chú giải khớp số.

**Không nên / điều kiện giới hạn:** Cú pháp phải đúng; không nhét cả file dài hoặc tự bịa schema.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ VÙNG MÃ 1 ] --- [ GIẢI THÍCH 1 ]
[ VÙNG MÃ 2 ] --- [ GIẢI THÍCH 2 ]
[ VÙNG MÃ 3 ] --- [ GIẢI THÍCH 3 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Cú pháp phải đúng; không nhét cả file dài hoặc tự bịa schema.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L13 — Giải phẫu tài liệu hoặc mã.
Mục tiêu: Tài liệu và 3–4 vùng chú giải khớp số.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Cú pháp phải đúng; không nhét cả file dài hoặc tự bịa schema.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Cú pháp phải đúng; không nhét cả file dài hoặc tự bịa schema.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Giải thích cấu trúc một tệp bằng vùng đánh dấu và chú giải.

```text
[TÀI LIỆU / MÃ 55%] — — [CHÚ GIẢI 40%]
```

**Ràng buộc:** Khoảng 5% cho đường dẫn. 3–4 vùng đánh dấu phải khớp số chú giải; đoạn mã ngắn và đúng cú pháp. Chữ nhỏ nên được ghép bằng lớp chỉnh sửa riêng.

**Ví dụ:** Các phần của SKILL.md: mô tả, đầu vào, bước xử lý, đầu ra.

**Nguồn bố cục:** Mở rộng từ L03 và ảnh giải phẫu SKILL.md đã trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L14 — Đặc tả bố cục

### L14 — Giao diện có chú giải

**Họ:** Giảng dạy. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Ảnh giao diện và 3–4 vùng thao tác được đánh số.

**Không nên / điều kiện giới hạn:** Mô phỏng cần ghi minh họa; không giả nút chức năng sản phẩm.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ NHÃN 1 ]—[ GIAO DIỆN ]—[ NHÃN 2 ]
              [ 3 / 4 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Mô phỏng cần ghi minh họa; không giả nút chức năng sản phẩm.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L14 — Giao diện có chú giải.
Mục tiêu: Ảnh giao diện và 3–4 vùng thao tác được đánh số.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Mô phỏng cần ghi minh họa; không giả nút chức năng sản phẩm.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Mô phỏng cần ghi minh họa; không giả nút chức năng sản phẩm.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Hướng dẫn nhận diện các vùng của phần mềm.

```text
[CHÚ GIẢI] — [GIAO DIỆN LỚN 01/02/03/04]
```

**Ràng buộc:** Tối đa bốn điểm gọi tên chính. Dùng ảnh chụp thật khi cần độ chính xác; bản vẽ ý tưởng phải được nhận diện là giao diện minh họa.

**Ví dụ:** Vùng hội thoại, trình quản lý tệp, vùng kết quả, vùng thực thi.

**Nguồn bố cục:** Mở rộng từ các ảnh mô tả không gian làm việc trong chuỗi trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L15 — Đặc tả bố cục

### L15 — Cây phân cấp

**Họ:** Kiến trúc. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Quan hệ cha–con 2–3 tầng; tối đa 7–9 nút cho tổng quan.

**Không nên / điều kiện giới hạn:** Không dùng cây cho mạng nhiều cha; cấp tên phải nhất quán.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
        [ GỐC ]
      ┌────┴────┐
    [ A ]    [ B ]
   ┌─┴─┐      └[ C ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không dùng cây cho mạng nhiều cha; cấp tên phải nhất quán.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L15 — Cây phân cấp.
Mục tiêu: Quan hệ cha–con 2–3 tầng; tối đa 7–9 nút cho tổng quan.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không dùng cây cho mạng nhiều cha; cấp tên phải nhất quán.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không dùng cây cho mạng nhiều cha; cấp tên phải nhất quán.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Trình bày cấu trúc thư mục, vai trò hoặc nhóm tác nhân.

```text
          [GỐC]
        /   |   \
      [A]  [B]  [C]
```

**Ràng buộc:** Khoảng 2–3 cấp; dùng nhánh cho quan hệ chứa/thuộc, không dùng mũi tên thời gian. Không giả định mọi nhánh có cùng quyền hạn.

**Ví dụ:** Cấu trúc thư mục đầu vào, đầu ra và tài liệu tham chiếu.

**Nguồn bố cục:** Mở rộng từ khung tư duy và mẫu phân cấp AIA-102.

PNG xem trước · SVG · Chỉ mục.

---

## L16 — Đặc tả bố cục

### L16 — Thang trưởng thành

**Họ:** Định vị. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

3–5 mức năng lực có điều kiện chuyển bậc.

**Không nên / điều kiện giới hạn:** Không hứa bậc cao nhất luôn phù hợp; chiều cao không giả số đo.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
                  [ 4 ]
            [ 3 ]
      [ 2 ]
[ 1 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không hứa bậc cao nhất luôn phù hợp; chiều cao không giả số đo.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L16 — Thang trưởng thành.
Mục tiêu: 3–5 mức năng lực có điều kiện chuyển bậc.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không hứa bậc cao nhất luôn phù hợp; chiều cao không giả số đo.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không hứa bậc cao nhất luôn phù hợp; chiều cao không giả số đo.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Mô tả tiến trình tăng năng lực, phạm vi hoặc độ phức tạp.

```text
[MỨC 1] _ [MỨC 2] __ [MỨC 3] ___ [MỨC 4]
```

**Ràng buộc:** Tên mức, điều kiện chuyển mức và ranh giới quyền phải rõ. Không mặc định mức cao hơn luôn phù hợp hơn.

**Ví dụ:** Các cấp độ tự chủ của tác nhân AI.

**Nguồn bố cục:** Mở rộng từ khung tư duy và mẫu thang cấp AIA-102.

PNG xem trước · SVG · Chỉ mục.

---

## L17 — Đặc tả bố cục

### L17 — Đầu vào – xử lý – đầu ra

**Họ:** Quy trình. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Các nguồn được xử lý thành sản phẩm công việc.

**Không nên / điều kiện giới hạn:** Ghi bước thủ công/duyệt; không gọi copy là tích hợp tự động.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ NGUỒN A ] ┐
[ NGUỒN B ] ┴→ [ XỬ LÝ ] → [ ĐẦU RA ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Ghi bước thủ công/duyệt; không gọi copy là tích hợp tự động.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L17 — Đầu vào – xử lý – đầu ra.
Mục tiêu: Các nguồn được xử lý thành sản phẩm công việc.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Ghi bước thủ công/duyệt; không gọi copy là tích hợp tự động.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Ghi bước thủ công/duyệt; không gọi copy là tích hợp tự động.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Giải thích sự phối hợp công cụ để tạo sản phẩm công việc.

```text
[TÀI LIỆU] → [TRA CỨU] → [SOẠN THẢO] → [BẢN DỰ THẢO]
```

**Ràng buộc:** Phân biệt bước chuyển thủ công với tự động; nêu kiểm tra trước phát hành nếu có. Không biến đầu ra AI thành văn bản đã được phê duyệt.

**Ví dụ:** Tra cứu căn cứ → Chuyển thông tin → Soạn tờ trình → Rà soát.

**Nguồn bố cục:** Mở rộng từ L05/L08 và ví dụ phối hợp công cụ trong chuỗi trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L18 — Đặc tả bố cục

### L18 — Chuỗi tình huống so le

**Họ:** Ứng dụng. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

4–5 ứng dụng độc lập theo nhịp cao–thấp.

**Không nên / điều kiện giới hạn:** Đường cong dẫn mắt không có mũi tên chỉ thứ tự giả.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ A ]       [ C ]       [ E ]
     [ B ]       [ D ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Đường cong dẫn mắt không có mũi tên chỉ thứ tự giả.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L18 — Chuỗi tình huống so le.
Mục tiêu: 4–5 ứng dụng độc lập theo nhịp cao–thấp.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Đường cong dẫn mắt không có mũi tên chỉ thứ tự giả.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Đường cong dẫn mắt không có mũi tên chỉ thứ tự giả.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Trình bày 4–5 ứng dụng độc lập mà vẫn có nhịp thị giác.

```text
[1]       [3]       [5]
     [2]       [4]
```

**Ràng buộc:** Đường sóng dẫn mắt, không hàm ý bắt buộc làm theo thứ tự. Độ chênh cao–thấp vừa phải để vẫn đọc trái sang phải.

**Ví dụ:** Năm ứng dụng AI trong công tác hành chính.

**Nguồn bố cục:** Kế thừa bố cục sóng uốn lượn ở mục 2.3, tài liệu 02.

PNG xem trước · SVG · Chỉ mục.

---

## L19 — Đặc tả bố cục

### L19 — Quy trình phân nhánh

**Họ:** Quy trình. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Một quyết định chính với 2–3 kết quả hoặc ngoại lệ.

**Không nên / điều kiện giới hạn:** Mỗi nhánh có nhãn; không đường cụt không giải thích.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ KIỂM TRA ] → ◇ ĐỦ?
               ├ CÓ → [ LÀM ]
               └ KHÔNG → [ BỔ SUNG ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Mỗi nhánh có nhãn; không đường cụt không giải thích.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L19 — Quy trình phân nhánh.
Mục tiêu: Một quyết định chính với 2–3 kết quả hoặc ngoại lệ.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Mỗi nhánh có nhãn; không đường cụt không giải thích.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Mỗi nhánh có nhãn; không đường cụt không giải thích.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Mô tả điều kiện, phê duyệt, ngoại lệ hoặc nhiều bên tham gia.

```text
[TIẾP NHẬN] → <ĐỦ ĐIỀU KIỆN?> → [THỰC HIỆN]
                      ↓ CHƯA ĐỦ
                 [BỔ SUNG / TRẢ LẠI]
```

**Ràng buộc:** Biến thể A: lưu đồ quyết định. Biến thể B: làn trách nhiệm theo người/đơn vị. Giới hạn 1–2 quyết định chính trên một slide; quy trình dài cần tổng quan và trang chi tiết.

**Ví dụ:** Tiếp nhận hồ sơ → Xét duyệt → Phân lớp → Theo dõi.

**Nguồn bố cục:** Mở rộng từ L05 và lưu đồ hành chính trường học đã trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L20 — Đặc tả bố cục

### L20 — Minh họa hoặc nền không chữ

**Họ:** Minh họa. **Nguồn:** Kế thừa bản 20, bổ sung đặc tả v2.

#### Mục tiêu và khi sử dụng

Ẩn dụ ở rìa, vùng trống lớn để chèn nội dung.

**Không nên / điều kiện giới hạn:** Cấm mọi chữ/số/ký tự, kể cả trên vật thể.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; giữ các vùng hình thuần thị giác trong safe margin của deck.
Dành khoảng 24–32 px giữa vùng; vùng trống và điểm nhấn lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ CHI TIẾT ] [ VÙNG TRỐNG ≥50% ] [ CHI TIẾT ]
```

#### Chữ, đường nối, màu và hình neo

Density bắt buộc là `D0`: không nhãn, không title, không body, không chú thích và không footer trên ảnh.
Chỉ dùng hình học, ánh sáng, vật liệu, khoảng trắng và vật thể không mang ký tự. Mọi nội dung chữ nếu có
thuộc artifact khác ngoài ảnh L20; không đưa nó vào prompt ImageGen của layout này.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Cấm mọi chữ/số/ký tự, kể cả trên vật thể.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L20 — Minh họa hoặc nền không chữ.
Mục tiêu: Ẩn dụ ở rìa, vùng trống lớn để chèn nội dung.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, palette, vật liệu, icon không chữ và bề mặt từ deck style lock.
Kiểm soát riêng: Cấm mọi chữ/số/ký tự, kể cả trên vật thể.
Không render bất kỳ chữ cái, chữ số, nhãn, từ, glyph dạng ký hiệu được dùng như chữ, tên tệp,
logo chữ, watermark, ký hiệu lập trình, ký tự thật hoặc ký tự giả nào, kể cả trên vật thể,
màn hình, sách, nhãn và chi tiết trang trí.
Chỉ dùng `NATIVE_IMAGEGEN_NO_TEXT`; không chọn chính sách sinh chữ, renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
L20 luôn là hình không chữ: giữ toàn bộ điều cấm ký tự trong prompt và kiểm ảnh thật theo workflow canonical.

#### Danh sách kiểm định riêng

- [ ] Cấm mọi chữ/số/ký tự, kể cả trên vật thể.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Không có chữ cái, chữ số, nhãn, từ, glyph dạng ký hiệu được dùng như chữ hoặc bất kỳ ký tự thật/giả nào; không có ngoại lệ chữ hiển thị.
- [ ] Lề, tương phản thị giác, vùng trống và việc không có chữ được kiểm tra trên ảnh thực.

#### Phần kế thừa từ bộ 20

**Dùng khi:** Tạo ẩn dụ, bìa, nền công nghệ hoặc hình neo riêng.

```text
[HẠT / SÓNG] → [VÙNG TRỐNG] → [CẤU TRÚC / CÔNG CỤ]
```

**Ràng buộc:** Không chữ, số, tên tệp hoặc ký hiệu lập trình; vùng nội dung trống 40–55% theo bố cục. Không bắt buộc có thẻ, số bước hoặc chân trang.

**Ví dụ:** Sóng ánh sáng chuyển thành cấu trúc thực thi; bộ não số ba tầng.

**Nguồn bố cục:** Mở rộng từ nhóm ảnh concept art và nền không chữ đã trao đổi.

PNG xem trước · SVG · Chỉ mục.

---

## L21 — Đặc tả bố cục

### L21 — Dòng thời gian sự kiện

**Họ:** Quy trình. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

4–6 mốc có thứ tự ngày hoặc giai đoạn rõ.

**Không nên / điều kiện giới hạn:** Không khoảng đều giả thời gian đều; ghi sơ đồ không theo tỷ lệ khi cần.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
    [ B ]         [ D ]
──●───●───●───●──→
[ A ]     [ C ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không khoảng đều giả thời gian đều; ghi sơ đồ không theo tỷ lệ khi cần.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L21 — Dòng thời gian sự kiện.
Mục tiêu: 4–6 mốc có thứ tự ngày hoặc giai đoạn rõ.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không khoảng đều giả thời gian đều; ghi sơ đồ không theo tỷ lệ khi cần.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không khoảng đều giả thời gian đều; ghi sơ đồ không theo tỷ lệ khi cần.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L22 — Đặc tả bố cục

### L22 — Làn trách nhiệm

**Họ:** Quy trình. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3 làn vai trò, 3–4 chặng, chỉ rõ bàn giao.

**Không nên / điều kiện giới hạn:** Bước ở đúng làn; đường vượt làn biểu thị chuyển giao có hướng.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
NGƯỜI DÙNG [ GIAO ] ─────── [ DUYỆT ]
TÁC NHÂN         [ XỬ LÝ ] ───┘
HỆ THỐNG             [ LƯU ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Bước ở đúng làn; đường vượt làn biểu thị chuyển giao có hướng.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L22 — Làn trách nhiệm.
Mục tiêu: 3 làn vai trò, 3–4 chặng, chỉ rõ bàn giao.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Bước ở đúng làn; đường vượt làn biểu thị chuyển giao có hướng.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Bước ở đúng làn; đường vượt làn biểu thị chuyển giao có hướng.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L23 — Đặc tả bố cục

### L23 — Phễu chuyển đổi

**Họ:** Dữ liệu. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3–5 tập con giảm dần hoặc tầng sàng lọc.

**Không nên / điều kiện giới hạn:** Bề rộng từ số thật khi định lượng; tỷ lệ có mẫu số/đơn vị.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[      BAN ĐẦU      ]
   [ SÀNG LỌC ]
      [ ĐẠT ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Bề rộng từ số thật khi định lượng; tỷ lệ có mẫu số/đơn vị.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L23 — Phễu chuyển đổi.
Mục tiêu: 3–5 tập con giảm dần hoặc tầng sàng lọc.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Bề rộng từ số thật khi định lượng; tỷ lệ có mẫu số/đơn vị.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Bề rộng từ số thật khi định lượng; tỷ lệ có mẫu số/đơn vị.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L24 — Đặc tả bố cục

### L24 — Kim tự tháp nền tảng

**Họ:** Kiến thức. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3–4 tầng nền tảng/phụ thuộc có ý nghĩa.

**Không nên / điều kiện giới hạn:** Không tạo thứ bậc cho nhóm ngang hàng; diện tích không giả tỷ trọng.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
        / ĐỈNH \
     / TẦNG GIỮA \
  /   NỀN TẢNG   \
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không tạo thứ bậc cho nhóm ngang hàng; diện tích không giả tỷ trọng.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L24 — Kim tự tháp nền tảng.
Mục tiêu: 3–4 tầng nền tảng/phụ thuộc có ý nghĩa.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không tạo thứ bậc cho nhóm ngang hàng; diện tích không giả tỷ trọng.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không tạo thứ bậc cho nhóm ngang hàng; diện tích không giả tỷ trọng.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L25 — Đặc tả bố cục

### L25 — Các vòng phạm vi

**Họ:** Kiến trúc. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3 lớp bao hàm, phạm vi tác động hoặc bảo vệ.

**Không nên / điều kiện giới hạn:** Bao hàm không phải tiến trình; nhãn phải khớp đúng vòng.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ NGOÀI ( GIỮA ( LÕI ) ) ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Bao hàm không phải tiến trình; nhãn phải khớp đúng vòng.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L25 — Các vòng phạm vi.
Mục tiêu: 3 lớp bao hàm, phạm vi tác động hoặc bảo vệ.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Bao hàm không phải tiến trình; nhãn phải khớp đúng vòng.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Bao hàm không phải tiến trình; nhãn phải khớp đúng vòng.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L26 — Đặc tả bố cục

### L26 — Vùng giao và chồng lấp

**Họ:** So sánh. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Hai hoặc ba tập có phần chung và riêng.

**Không nên / điều kiện giới hạn:** Không tạo giao giả; diện tích không là dữ liệu khi chỉ khái niệm.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
( TẬP A ) ∩ ( TẬP B )
      PHẦN CHUNG
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không tạo giao giả; diện tích không là dữ liệu khi chỉ khái niệm.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L26 — Vùng giao và chồng lấp.
Mục tiêu: Hai hoặc ba tập có phần chung và riêng.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không tạo giao giả; diện tích không là dữ liệu khi chỉ khái niệm.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không tạo giao giả; diện tích không là dữ liệu khi chỉ khái niệm.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L27 — Đặc tả bố cục

### L27 — Mạng lưới hệ sinh thái

**Họ:** Kiến trúc. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

5–7 thực thể kết nối chọn lọc, không một lõi duy nhất.

**Không nên / điều kiện giới hạn:** Không nối tất cả; có chú giải loại liên kết nếu khác nhau.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ A ]—[ B ]—[ C ]
  │   / │     │
[ D ]—[ E ]—[ F ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không nối tất cả; có chú giải loại liên kết nếu khác nhau.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L27 — Mạng lưới hệ sinh thái.
Mục tiêu: 5–7 thực thể kết nối chọn lọc, không một lõi duy nhất.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không nối tất cả; có chú giải loại liên kết nếu khác nhau.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không nối tất cả; có chú giải loại liên kết nếu khác nhau.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L28 — Đặc tả bố cục

### L28 — Tổng quan chỉ số

**Họ:** Dữ liệu. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3 KPI, 1–2 hình phân tích cùng thời kỳ.

**Không nên / điều kiện giới hạn:** Số, nguồn, đơn vị rõ; không KPI ngẫu nhiên như báo cáo thật.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ KPI A ] [ KPI B ] [ KPI C ]
[ BIỂU ĐỒ CHÍNH ] [ CƠ CẤU ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Số, nguồn, đơn vị rõ; không KPI ngẫu nhiên như báo cáo thật.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L28 — Tổng quan chỉ số.
Mục tiêu: 3 KPI, 1–2 hình phân tích cùng thời kỳ.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Số, nguồn, đơn vị rõ; không KPI ngẫu nhiên như báo cáo thật.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Số, nguồn, đơn vị rõ; không KPI ngẫu nhiên như báo cáo thật.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L29 — Đặc tả bố cục

### L29 — Một biểu đồ – một thông điệp

**Họ:** Dữ liệu. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Một biểu đồ làm bằng chứng cho tiêu đề và một chú thích.

**Không nên / điều kiện giới hạn:** Không 3D làm méo tỷ lệ; kiểm trục, phép tính, dữ liệu gốc.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ THÔNG ĐIỆP ]
[ BIỂU ĐỒ 70% ] [ CHÚ THÍCH 30% ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không 3D làm méo tỷ lệ; kiểm trục, phép tính, dữ liệu gốc.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L29 — Một biểu đồ – một thông điệp.
Mục tiêu: Một biểu đồ làm bằng chứng cho tiêu đề và một chú thích.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không 3D làm méo tỷ lệ; kiểm trục, phép tính, dữ liệu gốc.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không 3D làm méo tỷ lệ; kiểm trục, phép tính, dữ liệu gốc.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L30 — Đặc tả bố cục

### L30 — Thông điệp điều hành

**Họ:** Đúc kết. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Một kết luận, 2–3 bằng chứng và một quyết định cần chốt.

**Không nên / điều kiện giới hạn:** Bằng chứng phải hỗ trợ kết luận; không giấu rủi ro quan trọng.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ KẾT LUẬN ]
[ BẰNG CHỨNG A ] [ B ] [ C ]
[ HÀNH ĐỘNG ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Bằng chứng phải hỗ trợ kết luận; không giấu rủi ro quan trọng.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L30 — Thông điệp điều hành.
Mục tiêu: Một kết luận, 2–3 bằng chứng và một quyết định cần chốt.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Bằng chứng phải hỗ trợ kết luận; không giấu rủi ro quan trọng.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Bằng chứng phải hỗ trợ kết luận; không giấu rủi ro quan trọng.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L31 — Đặc tả bố cục

### L31 — Chuyển đổi trước – sau

**Họ:** So sánh. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Trạng thái hiện tại, cơ chế thay đổi và đích cùng tiêu chí.

**Không nên / điều kiện giới hạn:** Không trình bày trạng thái mới như hoàn hảo; ghi điều kiện triển khai.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ HIỆN TẠI ] == CƠ CHẾ ==> [ ĐỀ XUẤT ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không trình bày trạng thái mới như hoàn hảo; ghi điều kiện triển khai.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L31 — Chuyển đổi trước – sau.
Mục tiêu: Trạng thái hiện tại, cơ chế thay đổi và đích cùng tiêu chí.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không trình bày trạng thái mới như hoàn hảo; ghi điều kiện triển khai.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không trình bày trạng thái mới như hoàn hảo; ghi điều kiện triển khai.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L32 — Đặc tả bố cục

### L32 — Chuỗi cảnh minh họa

**Họ:** Ứng dụng. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3–4 cảnh người–công cụ, mỗi cảnh một hành động và đầu ra.

**Không nên / điều kiện giới hạn:** Nhất quán nhân vật; không thay sơ đồ kỹ thuật cần chính xác bằng cảnh mơ hồ.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ CẢNH 1 ] → [ CẢNH 2 ] → [ CẢNH 3 ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Nhất quán nhân vật; không thay sơ đồ kỹ thuật cần chính xác bằng cảnh mơ hồ.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L32 — Chuỗi cảnh minh họa.
Mục tiêu: 3–4 cảnh người–công cụ, mỗi cảnh một hành động và đầu ra.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Nhất quán nhân vật; không thay sơ đồ kỹ thuật cần chính xác bằng cảnh mơ hồ.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Nhất quán nhân vật; không thay sơ đồ kỹ thuật cần chính xác bằng cảnh mơ hồ.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L33 — Đặc tả bố cục

### L33 — Trích dẫn hoặc số liệu chủ đạo

**Họ:** Định vị. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Một số liệu lớn hoặc trích dẫn có nguồn để mở vấn đề.

**Không nên / điều kiện giới hạn:** Không số thiếu mẫu số/ngày; không bịa trích dẫn của chuyên gia.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[       SỐ LIỆU / TRÍCH DẪN       ]
[ PHẠM VI — Ý NGHĨA — NGUỒN       ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không số thiếu mẫu số/ngày; không bịa trích dẫn của chuyên gia.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L33 — Trích dẫn hoặc số liệu chủ đạo.
Mục tiêu: Một số liệu lớn hoặc trích dẫn có nguồn để mở vấn đề.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không số thiếu mẫu số/ngày; không bịa trích dẫn của chuyên gia.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không số thiếu mẫu số/ngày; không bịa trích dẫn của chuyên gia.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L34 — Đặc tả bố cục

### L34 — Lịch triển khai theo thời lượng

**Họ:** Quy trình. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3–5 hạng mục có điểm bắt đầu/kết thúc, chạy song song.

**Không nên / điều kiện giới hạn:** Thanh theo thời gian thật; không tự đặt SLA thiếu căn cứ.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
          KỲ 1 | KỲ 2 | KỲ 3 | KỲ 4
A         [=======]
B                [===========]
C                            [====]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Thanh theo thời gian thật; không tự đặt SLA thiếu căn cứ.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L34 — Lịch triển khai theo thời lượng.
Mục tiêu: 3–5 hạng mục có điểm bắt đầu/kết thúc, chạy song song.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Thanh theo thời gian thật; không tự đặt SLA thiếu căn cứ.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Thanh theo thời gian thật; không tự đặt SLA thiếu căn cứ.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L35 — Đặc tả bố cục

### L35 — Luồng phân bổ nguồn lực

**Họ:** Dữ liệu. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

Nguồn chia vào đích: khái niệm hoặc lượng cân đối.

**Không nên / điều kiện giới hạn:** Định lượng phải theo số/cân bằng; khái niệm ghi không theo tỷ lệ.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ NGUỒN A ] ==\
               [ XỬ LÝ ] == [ ĐÍCH X ]
[ NGUỒN B ] ==/          === [ ĐÍCH Y ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Định lượng phải theo số/cân bằng; khái niệm ghi không theo tỷ lệ.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L35 — Luồng phân bổ nguồn lực.
Mục tiêu: Nguồn chia vào đích: khái niệm hoặc lượng cân đối.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Định lượng phải theo số/cân bằng; khái niệm ghi không theo tỷ lệ.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Định lượng phải theo số/cân bằng; khái niệm ghi không theo tỷ lệ.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L36 — Đặc tả bố cục

### L36 — Nguyên nhân – hệ quả

**Họ:** Kiến thức. **Nguồn:** Đề xuất mở rộng của gói; tham khảo quan hệ tại S11–S12.

#### Mục tiêu và khi sử dụng

3–4 nhóm giả thuyết nguyên nhân cho một vấn đề cần kiểm tra.

**Không nên / điều kiện giới hạn:** Không biến giả thuyết thành nhân quả đã chứng minh; có bước kiểm chứng.

#### Cấu trúc 16:9 và sơ đồ khung

Khung tham chiếu 1920×1080; x=100–1820, tiêu đề y=90–210; nội dung y=250–900.
Dành khoảng 24–32 px giữa vùng. Các khối quá dày phải tách trang, không giảm chữ.
Vùng tiêu đề, lề và khoảng trống lấy từ deck style lock. Phân chia vùng trong sơ đồ
chỉ mô tả topology, không phải số liệu hoặc tọa độ tỷ lệ nghiệp vụ.

```text
[ A ] \     / [ B ]
──────────────→ [ VẤN ĐỀ ]
[ C ] /     \ [ D ]
```

#### Chữ, đường nối, màu và hình neo

Dùng 3–5 đơn vị chính khi phù hợp; trích dẫn chỉ một ý; cây tối đa 2–3 tầng cho tổng quan.
Mỗi khối một nhãn + tối đa hai ý ngắn, ngân sách toàn trang mặc định D1 35–70 đơn vị cách trắng; D2 71–100 chỉ dùng có chủ đích.
Không áp cứng số khối nếu làm mất bước thật. Thông tin bổ sung chuyển sang ghi chú giảng viên.

Đường có mũi tên biểu thị hướng, đường thường biểu thị liên hệ; không đường là nhóm độc lập.
Không biến giả thuyết thành nhân quả đã chứng minh; có bước kiểm chứng.

Nếu chưa có style lock, navy, xanh dương, cam và icon phẳng hai tông chỉ là một ví dụ
khởi tạo để người dùng duyệt. Deck style lock luôn ghi đè ví dụ này; màu và bề mặt không được lấn quan hệ/nhãn.

#### Prompt mẫu tiếng Việt

```text
Tạo infographic PowerPoint 16:9 theo L36 — Nguyên nhân – hệ quả.
Mục tiêu: 3–4 nhóm giả thuyết nguyên nhân cho một vấn đề cần kiểm tra.
Sơ đồ theo các vùng đã khóa, lề tham chiếu trái/phải 100, trên 90, dưới 80 px
trong khung 1920×1080. Lấy nền, font appearance, palette, icon và bề mặt từ deck style lock.
Kiểm soát riêng: Không biến giả thuyết thành nhân quả đã chứng minh; có bước kiểm chứng.
Chỉ hiển thị chuỗi trong CONTENT_LOCK đính kèm; giữ ngoại lệ tên riêng/cú pháp.
Không thêm tiếng Anh, số liệu, logo hay tuyên bố chức năng ngoài nội dung đã duyệt.
Không thu nhỏ chữ để nhét; nếu vượt ngân sách thì cần biên tập trước khi tạo ảnh.
Chỉ dùng direct text với native ImageGen; không chọn renderer mode hoặc fallback khác tại layout.
```

Điền biến bằng mẫu prompt, rồi preflight và gọi công cụ theo
workflow canonical.
Nếu brief yêu cầu hình không chữ, vẫn dùng native ImageGen và khóa rõ điều cấm ký tự; direct text là mặc định cho slide có chữ.

#### Danh sách kiểm định riêng

- [ ] Không biến giả thuyết thành nhân quả đã chứng minh; có bước kiểm chứng.
- [ ] Đủ khối/đúng quan hệ/đúng thứ tự và nhánh theo nguồn.
- [ ] Tiếng Việt chính xác; không sót chữ ngoài danh sách ngoại lệ.
- [ ] Lề, cỡ chữ, tương phản và vùng trống được kiểm tra trên ảnh thực.

PNG xem trước · SVG · Chỉ mục.

---

## L37 — Đặc tả bố cục

### L37 — Bản đồ nội dung chương

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Định hướng 3–4 chủ đề xoay quanh một câu hỏi dẫn đường.

**Không nên / kiểm soát:** Không dùng mũi tên tuần tự khi các chủ đề độc lập.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Câu hỏi của chương]
[Chủ đề A]  [Chủ đề B]
[Chủ đề C]  [Chủ đề D]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L37 — Bản đồ nội dung chương.
Mục tiêu: Định hướng 3–4 chủ đề xoay quanh một câu hỏi dẫn đường.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không dùng mũi tên tuần tự khi các chủ đề độc lập.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không dùng mũi tên tuần tự khi các chủ đề độc lập.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L38 — Đặc tả bố cục

### L38 — Công thức và ý nghĩa biến

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Một biểu thức trung tâm có 3–4 nhánh giải thích các thành phần.

**Không nên / kiểm soát:** Không minh họa công thức bằng dữ liệu hoặc hệ số tự tạo.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Biến A]    [Biến B]    [Biến C]
     \         |         /
          [Công thức]
          [Ý nghĩa]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L38 — Công thức và ý nghĩa biến.
Mục tiêu: Một biểu thức trung tâm có 3–4 nhánh giải thích các thành phần.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không minh họa công thức bằng dữ liệu hoặc hệ số tự tạo.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không minh họa công thức bằng dữ liệu hoặc hệ số tự tạo.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L39 — Đặc tả bố cục

### L39 — Chân dung và nhu cầu

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Một người sử dụng điển hình kết nối mục tiêu, trở ngại và hỗ trợ cần có.

**Không nên / kiểm soát:** Chân dung giả định phải được ghi nhãn; không suy diễn thuộc tính cá nhân từ ảnh.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Chân dung] | [Mục tiêu]
            | [Trở ngại]
            | [Cần hỗ trợ]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L39 — Chân dung và nhu cầu.
Mục tiêu: Một người sử dụng điển hình kết nối mục tiêu, trở ngại và hỗ trợ cần có.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Chân dung giả định phải được ghi nhãn; không suy diễn thuộc tính cá nhân từ ảnh.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Chân dung giả định phải được ghi nhãn; không suy diễn thuộc tính cá nhân từ ảnh.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L40 — Đặc tả bố cục

### L40 — Cân đối lợi ích và đánh đổi

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Hai mục tiêu cạnh tranh đặt trên trục đánh đổi với điều kiện quyết định.

**Không nên / kiểm soát:** Không dùng độ cao hoặc độ lớn để ngầm chấm điểm khi chưa có dữ liệu.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Lợi ích A]  <— ĐÁNH ĐỔI —> [Lợi ích B]
          [Điều kiện lựa chọn]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L40 — Cân đối lợi ích và đánh đổi.
Mục tiêu: Hai mục tiêu cạnh tranh đặt trên trục đánh đổi với điều kiện quyết định.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không dùng độ cao hoặc độ lớn để ngầm chấm điểm khi chưa có dữ liệu.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không dùng độ cao hoặc độ lớn để ngầm chấm điểm khi chưa có dữ liệu.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L41 — Đặc tả bố cục

### L41 — Bảng tiêu chí lựa chọn

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Đối chiếu 2–3 phương án bằng 3–4 tiêu chí với ô đủ/chưa đủ rõ ràng.

**Không nên / kiểm soát:** Không tự gán điểm tổng hoặc phương án thắng; nêu dữ kiện còn thiếu.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Tiêu chí] | [A] | [B]
[Phù hợp]  |  ✓  |  ?
[Chi phí]  |  ?  |  ✓
[Kiểm soát]|  ✓  |  ✓
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L41 — Bảng tiêu chí lựa chọn.
Mục tiêu: Đối chiếu 2–3 phương án bằng 3–4 tiêu chí với ô đủ/chưa đủ rõ ràng.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không tự gán điểm tổng hoặc phương án thắng; nêu dữ kiện còn thiếu.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không tự gán điểm tổng hoặc phương án thắng; nêu dữ kiện còn thiếu.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L42 — Đặc tả bố cục

### L42 — Cổng kiểm soát theo giai đoạn

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Các vùng chuẩn bị, phê duyệt và triển khai được ngăn bởi điểm dừng có điều kiện.

**Không nên / kiểm soát:** Không nối tắt qua cổng duyệt hoặc coi phê duyệt là một trang trí.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Chuẩn bị] → <ĐỦ?> → [Phê duyệt] → <DUYỆT?> → [Triển khai]
                ↓                       ↓
            [Bổ sung]               [Điều chỉnh]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L42 — Cổng kiểm soát theo giai đoạn.
Mục tiêu: Các vùng chuẩn bị, phê duyệt và triển khai được ngăn bởi điểm dừng có điều kiện.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không nối tắt qua cổng duyệt hoặc coi phê duyệt là một trang trí.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không nối tắt qua cổng duyệt hoặc coi phê duyệt là một trang trí.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L43 — Đặc tả bố cục

### L43 — Ranh giới quyền và trách nhiệm

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Một vùng làm việc được phép và một vùng tác động bên ngoài, nối qua phê duyệt.

**Không nên / kiểm soát:** Không dùng vùng bao như bảo đảm an toàn; chỉ rõ đó là phạm vi được quy định.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Trong phạm vi cho phép]
[Đọc] [Soạn nháp] [Kiểm tra]
          ↓ <PHÊ DUYỆT>
[Ngoài phạm vi: gửi / công bố]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L43 — Ranh giới quyền và trách nhiệm.
Mục tiêu: Một vùng làm việc được phép và một vùng tác động bên ngoài, nối qua phê duyệt.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không dùng vùng bao như bảo đảm an toàn; chỉ rõ đó là phạm vi được quy định.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không dùng vùng bao như bảo đảm an toàn; chỉ rõ đó là phạm vi được quy định.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L44 — Đặc tả bố cục

### L44 — Cầu nối biến động chỉ số

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Giải thích giá trị đầu kỳ, đóng góp tăng giảm và giá trị cuối kỳ.

**Không nên / kiểm soát:** Tổng phải khớp; số ví dụ không được trình bày như dữ liệu thật.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Đầu] → [+Tăng] → [−Giảm] → [Cuối]
         Tổng các biến động phải khớp
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L44 — Cầu nối biến động chỉ số.
Mục tiêu: Giải thích giá trị đầu kỳ, đóng góp tăng giảm và giá trị cuối kỳ.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Tổng phải khớp; số ví dụ không được trình bày như dữ liệu thật.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Tổng phải khớp; số ví dụ không được trình bày như dữ liệu thật.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L45 — Đặc tả bố cục

### L45 — Cây yếu tố tác động chỉ tiêu

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Tách một chỉ tiêu thành các yếu tố định nghĩa/tác động có công thức hoặc giả thuyết rõ.

**Không nên / kiểm soát:** Phân biệt quan hệ tính toán với quan hệ nhân quả; không trộn hai loại.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
             [Chỉ tiêu]
            /          \
      [Yếu tố A]     [Yếu tố B]
      /     \         /      \
    [A1]   [A2]     [B1]    [B2]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L45 — Cây yếu tố tác động chỉ tiêu.
Mục tiêu: Tách một chỉ tiêu thành các yếu tố định nghĩa/tác động có công thức hoặc giả thuyết rõ.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Phân biệt quan hệ tính toán với quan hệ nhân quả; không trộn hai loại.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Phân biệt quan hệ tính toán với quan hệ nhân quả; không trộn hai loại.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L46 — Đặc tả bố cục

### L46 — Ba lỗi và cách sửa

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Ba cặp lỗi–cách sửa theo cùng hàng để hỗ trợ tự kiểm tra.

**Không nên / kiểm soát:** Phê bình thao tác, không gán nhãn xấu cho người học.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Lỗi 1] → [Cách sửa 1]
[Lỗi 2] → [Cách sửa 2]
[Lỗi 3] → [Cách sửa 3]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L46 — Ba lỗi và cách sửa.
Mục tiêu: Ba cặp lỗi–cách sửa theo cùng hàng để hỗ trợ tự kiểm tra.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Phê bình thao tác, không gán nhãn xấu cho người học.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Phê bình thao tác, không gán nhãn xấu cho người học.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L47 — Đặc tả bố cục

### L47 — Kiến thức – minh họa – thực hành

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Một nguyên lý được nối với ví dụ ngắn và yêu cầu tự làm.

**Không nên / kiểm soát:** Không điền sẵn toàn bộ đáp án phần thực hành.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Hiểu nguyên lý] → [Xem ví dụ] → [Tự thực hiện]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L47 — Kiến thức – minh họa – thực hành.
Mục tiêu: Một nguyên lý được nối với ví dụ ngắn và yêu cầu tự làm.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không điền sẵn toàn bộ đáp án phần thực hành.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không điền sẵn toàn bộ đáp án phần thực hành.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## L48 — Đặc tả bố cục

### L48 — Kế hoạch hành động một trang

**Trạng thái:** Bố cục mới do bộ thiết kế đề xuất trong v2.1.0; không phải mẫu có sẵn của PowerPoint.

#### Mục đích và giới hạn

Ba cam kết hành động có người phụ trách, thời điểm và điều kiện hoàn thành.

**Không nên / kiểm soát:** Không tự gán người hoặc hạn chót chưa được người dùng xác nhận.

#### Cấu trúc tham chiếu

16:9; khung 1920 × 1080; lề trái/phải 100 px, trên 90 px, dưới 80 px.
Chữ chính theo mục tiêu 20–24 pt khi dàn PowerPoint. Không co chữ để nhét thêm nội dung.

```text
[Việc cần làm] | [Phụ trách] | [Mốc] | [Hoàn thành]
[Việc 1]       | [Vai trò]  | [...] | [Bằng chứng]
[Việc 2]       | [Vai trò]  | [...] | [Bằng chứng]
```

Một quan hệ chính, 3–4 vùng nhận thức. Tiêu đề tối đa hai dòng. Tài liệu giải thích đưa ra ghi chú.
Ngân sách mật độ là quy ước biên tập của gói, không phải định luật nhận thức.

#### Prompt mẫu

```text
Thiết kế slide 16:9 tiếng Việt theo L48 — Kế hoạch hành động một trang.
Mục tiêu: Ba cam kết hành động có người phụ trách, thời điểm và điều kiện hoàn thành.
Nếu chưa có style lock, nền trắng, chữ navy và một màu nhấn chỉ là ví dụ khởi tạo;
deck style lock luôn ghi đè palette, font appearance, icon và bề mặt của ví dụ.
Bố trí theo sơ đồ khung đã chọn. Lề an toàn rộng; chỉ hiển thị CONTENT_LOCK đã duyệt.
Không tự gán người hoặc hạn chót chưa được người dùng xác nhận.
Không thêm số liệu, logo, chữ tiếng Anh hoặc lời hứa ngoài nội dung nguồn.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Một thông điệp chính, nhãn rõ và không vượt lề.
- [ ] Không tự gán người hoặc hạn chót chưa được người dùng xác nhận.
- [ ] Đọc được nhãn khi mở ảnh ở kích thước trình chiếu, không chỉ khi phóng to.
- [ ] Ví dụ được nhận diện; nội dung chuyên môn không được tự thêm.

Xem PNG · Xem SVG · Danh mục phân loại

---

## I01 — Đặc tả infographic

### I01 — Vòng lặp có kiểm duyệt

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L12, L19. **Nhóm:** G09A.
**Nguồn thị giác:** R01, R06 trong sổ ảnh tham chiếu.

#### Ý đồ

Bốn bước quanh tâm phê duyệt; mũi tên một chiều tạo chu trình kín.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Vòng lặp có kiểm duyệt

| Thành phần | Chữ trong thẻ |
|---|---|
| Lập kế hoạch | Xác định mục tiêu |
| Thực hiện | Làm trong phạm vi |
| Quan sát | Đọc kết quả |
| Đúc kết | Rút bài học |

**Thông điệp phụ/tâm:** Con người kiểm duyệt

**Lưu ý:** Rà soát trước hành động quan trọng.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I01 — Vòng lặp có kiểm duyệt.
Thiết kế: Bốn bước quanh tâm phê duyệt; mũi tên một chiều tạo chu trình kín.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I01 — Vòng lặp có kiểm duyệt; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I02 — Đặc tả infographic

### I02 — Bốn bước có hình minh họa

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L05. **Nhóm:** G09A.
**Nguồn thị giác:** R05 trong sổ ảnh tham chiếu.

#### Ý đồ

Bốn thẻ ngang có số, biểu tượng lớn, một câu hành động và đầu ra ngắn.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Bốn bước có hình minh họa

| Thành phần | Chữ trong thẻ |
|---|---|
| Sắp xếp | Gom dữ liệu nguồn |
| Đặt quy tắc | Giới hạn phạm vi |
| Đóng gói | Viết hướng dẫn |
| Kiểm thử | Đối chiếu kết quả |

**Thông điệp phụ/tâm:** Từ yêu cầu đến cách làm rõ ràng

**Lưu ý:** Mỗi bước có một đầu ra cụ thể.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I02 — Bốn bước có hình minh họa.
Thiết kế: Bốn thẻ ngang có số, biểu tượng lớn, một câu hành động và đầu ra ngắn.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I02 — Bốn bước có hình minh họa; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I03 — Đặc tả infographic

### I03 — Lộ trình ba chặng

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L02, L16. **Nhóm:** G09A.
**Nguồn thị giác:** R07 trong sổ ảnh tham chiếu.

#### Ý đồ

Ba chặng minh họa với bệ nền tăng nhẹ và đường nối hướng tiến.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Lộ trình ba chặng

| Thành phần | Chữ trong thẻ |
|---|---|
| Tập trung | Gom tri thức |
| Chuẩn hóa | Thống nhất cách làm |
| Nhân rộng | Chia sẻ cho nhóm |

**Thông điệp phụ/tâm:** Từ tri thức đến năng lực dùng chung

**Lưu ý:** Hoàn thiện nền tảng trước khi mở rộng.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I03 — Lộ trình ba chặng.
Thiết kế: Ba chặng minh họa với bệ nền tăng nhẹ và đường nối hướng tiến.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I03 — Lộ trình ba chặng; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I04 — Đặc tả infographic

### I04 — Năm bước với điểm duyệt

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L05, L42. **Nhóm:** G09A.
**Nguồn thị giác:** R05 trong sổ ảnh tham chiếu.

#### Ý đồ

Năm bước ngắn, bước duyệt cam nổi bật; chữ ít, đường nối không đi tắt.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Năm bước với điểm duyệt

| Thành phần | Chữ trong thẻ |
|---|---|
| Tìm hiểu | Đọc nguồn |
| Lập kế hoạch | Nêu thay đổi |
| Phê duyệt | Chốt phạm vi |
| Thực thi | Sửa đúng chỗ |
| Xác minh | Kiểm tra đầu ra |

**Thông điệp phụ/tâm:** Kiểm soát trước khi thay đổi

**Lưu ý:** Chưa được duyệt: dừng và làm rõ.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I04 — Năm bước với điểm duyệt.
Thiết kế: Năm bước ngắn, bước duyệt cam nổi bật; chữ ít, đường nối không đi tắt.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I04 — Năm bước với điểm duyệt; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I05 — Đặc tả infographic

### I05 — Từ hồ sơ đến sản phẩm

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L17. **Nhóm:** G09A.
**Nguồn thị giác:** R03, R05 trong sổ ảnh tham chiếu.

#### Ý đồ

Hồ sơ bên trái, bộ xử lý ở tâm, sản phẩm bên phải; có biểu tượng thực tế.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Từ hồ sơ đến sản phẩm

| Thành phần | Chữ trong thẻ |
|---|---|
| Dữ liệu gốc | Tài liệu đã chọn |
| Xử lý | Tổng hợp có căn cứ |
| Báo cáo | Bản nháp để duyệt |

**Thông điệp phụ/tâm:** Chuyển dữ liệu thành sản phẩm công việc

**Lưu ý:** Giữ đường dẫn tới tài liệu nguồn.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I05 — Từ hồ sơ đến sản phẩm.
Thiết kế: Hồ sơ bên trái, bộ xử lý ở tâm, sản phẩm bên phải; có biểu tượng thực tế.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I05 — Từ hồ sơ đến sản phẩm; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I06 — Đặc tả infographic

### I06 — Phối hợp con người và AI

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L22. **Nhóm:** G09A.
**Nguồn thị giác:** R01, R03 trong sổ ảnh tham chiếu.

#### Ý đồ

Hai làn ít bước với hình người và lõi AI, thể hiện giao việc và kiểm duyệt.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Phối hợp con người và AI

| Thành phần | Chữ trong thẻ |
|---|---|
| Giao mục tiêu | Nêu yêu cầu |
| Soạn phương án | Tạo bản nháp |
| Phê duyệt | Rà soát phạm vi |
| Hoàn thiện | Ghi nhận kết quả |

**Thông điệp phụ/tâm:** Phân vai rõ trong từng chặng

**Lưu ý:** AI hỗ trợ; con người chốt quyết định.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I06 — Phối hợp con người và AI.
Thiết kế: Hai làn ít bước với hình người và lõi AI, thể hiện giao việc và kiểm duyệt.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I06 — Phối hợp con người và AI; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I07 — Đặc tả infographic

### I07 — Ngôi nhà năm trụ cột

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L07, L10. **Nhóm:** G09B.
**Nguồn thị giác:** R04 trong sổ ảnh tham chiếu.

#### Ý đồ

Mái thông điệp, năm trụ độc lập có icon và nền chung; không giả quan hệ tuần tự.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Ngôi nhà năm trụ cột

| Thành phần | Chữ trong thẻ |
|---|---|
| Ngữ cảnh | Đúng nguồn |
| Quy tắc | Rõ phạm vi |
| Quy trình | Rõ các bước |
| Công cụ | Đúng quyền |
| Kiểm tra | Có bằng chứng |

**Thông điệp phụ/tâm:** Khung vận hành được kiểm soát

**Lưu ý:** Các trụ bổ trợ, không phải thứ tự thực hiện.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I07 — Ngôi nhà năm trụ cột.
Thiết kế: Mái thông điệp, năm trụ độc lập có icon và nền chung; không giả quan hệ tuần tự.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I07 — Ngôi nhà năm trụ cột; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I08 — Đặc tả infographic

### I08 — Kiến trúc ba tầng minh họa

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L07. **Nhóm:** G09B.
**Nguồn thị giác:** R03 trong sổ ảnh tham chiếu.

#### Ý đồ

Ba tầng ngang, hình người/AI ở tầng đầu, khối năng lực ở giữa và tài nguyên ở đáy.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Kiến trúc ba tầng minh họa

| Thành phần | Chữ trong thẻ |
|---|---|
| Con người | Đặt mục tiêu và duyệt |
| Nền tảng | Điều phối công việc |
| Tài nguyên | Tệp, công cụ, dữ liệu |

**Thông điệp phụ/tâm:** Từ mục tiêu đến nguồn lực thực thi

**Lưu ý:** Mỗi tầng có trách nhiệm riêng.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I08 — Kiến trúc ba tầng minh họa.
Thiết kế: Ba tầng ngang, hình người/AI ở tầng đầu, khối năng lực ở giữa và tài nguyên ở đáy.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I08 — Kiến trúc ba tầng minh họa; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I09 — Đặc tả infographic

### I09 — Thang năng lực bốn mức

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L16. **Nhóm:** G09B.
**Nguồn thị giác:** R02 trong sổ ảnh tham chiếu.

#### Ý đồ

Bốn thẻ bậc thang có biểu tượng lớn, không gắn phần trăm tự chủ giả định.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Thang năng lực bốn mức

| Thành phần | Chữ trong thẻ |
|---|---|
| Gợi ý | Người dùng thực hiện |
| Hỗ trợ | AI soạn bản nháp |
| Có kiểm soát | Thực thi trong phạm vi |
| Phối hợp | Nhiều bước có kiểm duyệt |

**Thông điệp phụ/tâm:** Tăng năng lực, giữ cơ chế giám sát

**Lưu ý:** Khung minh họa, không phải chuẩn phân cấp.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I09 — Thang năng lực bốn mức.
Thiết kế: Bốn thẻ bậc thang có biểu tượng lớn, không gắn phần trăm tự chủ giả định.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I09 — Thang năng lực bốn mức; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I10 — Đặc tả infographic

### I10 — Lõi và ba hệ thống hỗ trợ

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L11. **Nhóm:** G09B.
**Nguồn thị giác:** R03 trong sổ ảnh tham chiếu.

#### Ý đồ

Lõi hình minh họa ở tâm, ba khối lập kế hoạch, tri thức, công cụ nối có nhãn.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Lõi và ba hệ thống hỗ trợ

| Thành phần | Chữ trong thẻ |
|---|---|
| Lập kế hoạch | Chia nhiệm vụ |
| Tri thức | Tra cứu nguồn |
| Công cụ | Thực hiện thao tác |

**Thông điệp phụ/tâm:** Lõi AI

**Lưu ý:** Trao đổi theo nhiệm vụ và quyền đã cấp.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I10 — Lõi và ba hệ thống hỗ trợ.
Thiết kế: Lõi hình minh họa ở tâm, ba khối lập kế hoạch, tri thức, công cụ nối có nhãn.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I10 — Lõi và ba hệ thống hỗ trợ; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I11 — Đặc tả infographic

### I11 — Hai cách làm trước và sau

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L31, L06. **Nhóm:** G09B.
**Nguồn thị giác:** R07 trong sổ ảnh tham chiếu.

#### Ý đồ

Hai cảnh minh họa trước–sau cân đối, giữa là mũi tên chuyển đổi có điều kiện.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Hai cách làm trước và sau

| Thành phần | Chữ trong thẻ |
|---|---|
| Rời rạc | Trao đổi từng lần |
| Có cấu trúc | Nguồn và cách làm dùng chung |

**Thông điệp phụ/tâm:** Từ xử lý riêng lẻ đến quy trình dùng lại

**Lưu ý:** Chuyển đổi bắt đầu từ việc chuẩn hóa.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I11 — Hai cách làm trước và sau.
Thiết kế: Hai cảnh minh họa trước–sau cân đối, giữa là mũi tên chuyển đổi có điều kiện.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I11 — Hai cách làm trước và sau; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục

---

## I12 — Đặc tả infographic

### I12 — Bốn năng lực có biểu tượng

**Loại:** Mẫu infographic minh họa hoàn chỉnh với nội dung ví dụ, không chỉ khung SmartArt.
**Cấu trúc nền:** L10, L03. **Nhóm:** G09B.
**Nguồn thị giác:** R04, R05 trong sổ ảnh tham chiếu.

#### Ý đồ

Lưới 2×2 thẻ có icon lớn, nhãn và một câu ngắn; không nối mũi tên giữa thẻ.

Đây là đề xuất thiết kế mới; ảnh nguồn được học về cách dùng icon, khối và khoảng cách.
Không kế thừa các tỷ lệ tự chủ, cam kết tuyệt đối hoặc tên giao diện như dữ kiện đã xác minh.

#### Chữ mẫu đã khóa

**Tiêu đề:** Bốn năng lực có biểu tượng

| Thành phần | Chữ trong thẻ |
|---|---|
| Đọc tài liệu | Tìm đúng nguồn |
| Lập kế hoạch | Chia việc rõ ràng |
| Thực hiện | Dùng công cụ phù hợp |
| Kiểm chứng | So khớp tiêu chí |

**Thông điệp phụ/tâm:** Bộ năng lực hỗ trợ công việc

**Lưu ý:** Tùy nhiệm vụ, chọn năng lực phù hợp.

Toàn bộ là ví dụ biên tập để xem thiết kế; khi triển khai thay bằng dữ liệu và chữ đã duyệt.
Trong ảnh xem trước có thể lược thông điệp phụ nếu trùng tiêu đề, để giữ mật độ nhẹ.

#### Đặc tả hình

Canvas 16:9. Nền trắng, navy, xanh dương, teal và cam chỉ là palette ví dụ khi chưa có style lock;
deck style lock luôn ghi đè màu, font appearance, icon và bề mặt của ví dụ.
Biểu tượng bán phẳng, chuyển sắc nhẹ và bóng mờ nhỏ; hình có ngữ nghĩa, không robot trang trí mọi ô.
3–5 khối chính. Mỗi khối một biểu tượng, một nhãn 2–5 đơn vị cách trắng và một câu 4–8 đơn vị.
Ngân sách mặc định D1: 35–70 đơn vị cách trắng trên slide, không tính mã mẫu của thư viện.
Đây là quy ước của project, không phải giới hạn nhận thức hay số token của mô hình.
Tiêu đề và lời mô tả không được chồng lên đường nối. Không đưa đoạn văn ra ngoài vòng làm tăng tải chữ.
Không thêm đồng thời hàng lợi ích, hàng ví dụ và hàng kết luận ở chân.

#### Prompt tạo hình

```text
Tạo infographic 16:9 tiếng Việt, nền trắng, mẫu I12 — Bốn năng lực có biểu tượng.
Thiết kế: Lưới 2×2 thẻ có icon lớn, nhãn và một câu ngắn; không nối mũi tên giữa thẻ.
Giữ deck style lock; nếu chưa có thì dùng palette ví dụ navy–xanh dương–teal và nhấn cam tiết chế để người dùng duyệt.
Dùng lề an toàn; chữ đậm, rõ dấu; 3–5 khối, mỗi khối một nhãn và một câu ngắn.
Chỉ vẽ văn bản trong bản khóa chữ đi kèm. Không thêm tiếng Anh, số %, khẳng định chưa kiểm chứng.
Không chỉ vẽ khung trống: phải có biểu tượng mang nghĩa và nội dung ví dụ ngắn.
Bản mẫu thư viện đánh rõ I12 — Bốn năng lực có biểu tượng; bản trình chiếu cuối bỏ mã nếu người dùng yêu cầu.
```

Prompt thực phải được biên soạn thành bảy phần và gọi native ImageGen theo
workflow canonical; layout không chọn renderer mode.

#### Kiểm định

- [ ] Có biểu tượng, nội dung ngắn và quan hệ đúng; không chỉ hộp rỗng.
- [ ] Không sao chép mật độ chữ cao của ảnh nguồn.
- [ ] Không có tỷ lệ hoặc cam kết tuyệt đối chưa được nguồn hỗ trợ.
- [ ] Mã đúng; đủ các thành phần; không cắt nhãn ở mép.

Xem PNG · Xem SVG · Danh mục
